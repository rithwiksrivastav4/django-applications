apiVersion: v1
kind: Service
metadata:
  name: django-app
  namespace: production
  labels:
    app: django-app
spec:
  type: ClusterIP
  selector:
    app: django-app
  ports:
    - name: http
      port: 80        # external port inside the cluster
      targetPort: 8000
      protocol: TCP
